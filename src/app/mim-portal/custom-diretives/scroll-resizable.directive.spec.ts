import { ScrollResizableDirective } from './scroll-resizable.directive';

describe('ScrollResizableDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollResizableDirective();
    expect(directive).toBeTruthy();
  });
});
